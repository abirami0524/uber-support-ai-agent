# Hiver

A small Python workspace for downloading and exploring the `thoughtvector/customer-support-on-twitter` Kaggle dataset through `kagglehub`.

## Usage

```python
from explore import download_dataset

path = download_dataset("thoughtvector/customer-support-on-twitter")
print(path)
```

## Install

```bash
pip install -r requirements.txt
```
